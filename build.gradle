buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath "tech.kronicle:metadata:$kronicleVersion"
    }
}

plugins {
    id "base"
}

apply from: "gradle/kronicle-metadata.gradle"
